<%= form_for @changeset, @action, fn f -> %>
  <%= if @changeset.action do %>
    <div class="alert alert-danger">
      <p>Oops, something went wrong! Please check the errors below.</p>
    </div>
  <% end %>

  <%= label f, :num_strokes %>
  <%= number_input f, :num_strokes %>
  <%= error_tag f, :num_strokes %>

  <div>
    <%= submit "Save" %>
  </div>
<% end %>
